name: water_meassurement
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  get: ^4.3.8
  intl: ^0.17.0
  uuid: ^3.0.4
  searchfield: ^0.5.5
  sqflite: ^2.0.0+4
  path: ^1.8.0
  provider: ^6.0.0
  image_picker: ^0.8.4

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/images/
